#include "pch.h"
#include "DomainHttpAccessCounter.h"

TEST(DomainHttpAccessCounter, AddNewDomain) {
  EXPECT_EQ(1, 1);
  EXPECT_TRUE(true);
}

TEST(DomainConsumptionSniffer, IncrementExistingDomain) {
	EXPECT_EQ(1, 1);
	EXPECT_TRUE(true);
}

TEST(DomainConsumptionSniffer, PrintDomainConsuption) {
	EXPECT_EQ(1, 1);
	EXPECT_TRUE(true);
}

TEST(TestCaseName, TestName) {
	EXPECT_EQ(1, 1);
	EXPECT_TRUE(true);
}